[project]
name = "http-event-collector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask",
    "pygments",
    "python-dotenv",
    "requests"
]
