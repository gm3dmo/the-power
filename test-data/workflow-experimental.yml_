name: the-power-workflow-experimental
on:
  pull_request:
    types: [opened ]
jobs:
  blank:
    runs-on: ubuntu-latest
    environment: environment_1
    steps:
      - name: Say Hello
        run: |
          echo Hello
          echo World
      - name: List directory
        run: |
          ls -l
