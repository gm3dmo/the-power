
name: the-power-workflow-merge-queue-example
on:
    pull_request:
    merge_group:
jobs:
  pwr-merge-queue-demo:
    runs-on: ubuntu-latest
    steps:
      - name: check-curl-version
        run: |
          curl --version
      - name: sleep-60
        run: |
          sleep 60

